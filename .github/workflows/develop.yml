name: Deploy to Development
on:
  push:
    branches:
      - develop
jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [19.x]

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"
      - run: npm ci
      - run: npm test
      - run: npm run build

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v2
        with:
          name: build
          path: build

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/develop' && github.event_name == 'push'

    steps:
      - uses: actions/checkout@v3
      - name: Download Build Artifact
        uses: actions/download-artifact@v3
        with:
          name: build
          path: build

      - name: remove old build
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.DEV_SERVER }}
          username: ${{ secrets.SSHUSERNAME }}
          password: ${{ secrets.SSHPASSWORD }}
          script: |
            echo ${{ secrets.SSHPASSWORD }} | sudo -S rm -rf /var/www/badging.chaoss.io/*
            mkdir -p /var/www/badging.chaoss.io

      - name: deploy new build
        uses: garygrossgarten/github-action-scp@release
        with:
          host: ${{ secrets.DEV_SERVER }}
          username: ${{ secrets.SSHUSERNAME }}
          password: ${{ secrets.SSHPASSWORD }}
          local: ${{ github.workspace }}/build
          remote: "/var/www/badging.chaoss.io"

      - name: restart nginx
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.DEV_SERVER }}
          username: ${{ secrets.SSHUSERNAME }}
          password: ${{ secrets.SSHPASSWORD }}
          script: |
            echo ${{ secrets.SSHPASSWORD }} | sudo -S systemctl restart nginx
